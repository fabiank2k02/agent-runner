- Send raw data to a centralized server and then process it in between frontend and backend instance (e.g. on a centralized backend or worker edge)
-> Make the smaller model learn from context and improve predictions/functioning in that way

- Improve the capability of starting multi jobs on one repo and merging it later (without eliminating the possibility of transferring dirty work trees with env files)